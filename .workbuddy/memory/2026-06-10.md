# 2026-06-10 Vol.7 终极安全审计

## 完成事项
- Vol.7 白帽攻击：12 个漏洞修复 + 2 个自检补丁
- 安全评分从 9.8 提升到 **9.9/10**
- 累计 7 轮审计修复 **54 个漏洞**（10 CRITICAL + 14 HIGH + 14 MEDIUM + 16 LOW）

## Vol.7 修复清单
| 编号 | 严重度 | 修复内容 |
|------|--------|---------|
| V7-01 | HIGH | `/unpair` 缺少 POST 强制 — CSRF 可解绑所有设备 |
| V7-02 | HIGH | 空指纹绕过白名单 — `isEmpty → true` 逻辑修复 |
| V7-03 | HIGH | `getHardwareFingerprint()` 失败返回空串 — 改为 sentinel |
| V7-04 | HIGH | HMAC/AES 密钥复用 — HKDF-SHA256 派生独立 HMAC 密钥 |
| V7-05 | MEDIUM | `/pair` 405 返回纯文本 — 统一 JSON 格式 |
| V7-06 | MEDIUM | `_isTestMode()` 生产环境可用 — kDebugMode 编译时门控 |
| V7-07 | MEDIUM | `_clearActivation()` 不删 AES/HMAC 密钥 — 自毁后密钥残留 |
| V7-08 | MEDIUM | `_saveActivation()` 用 SharedPreferences — 迁移 FlutterSecureStorage |
| V7-09 | MEDIUM | DataChannel 无消息速率限制 — 添加 30msg/s 限流 |
| V7-10 | MEDIUM | `sendCommand()` 无出站校验 — 白名单验证 |
| V7-11 | MEDIUM | `roomId` 无输入验证 — 正则校验防信令注入 |
| V7-12 | MEDIUM | `isBeingDebugged()` fail-open — 改为 fail-closed |
| V7-自检-01 | MEDIUM | 空 Host 头绕过 DNS Rebinding 检查 — `isEmpty → false` |
| V7-补丁 | MEDIUM | DynamicFirewall.isBeingDebugged() catch 块与 V7-12 不一致 |

## Git 提交
- `f0826a1` / `0efff89` — Vol.7 主修复
- `65cb413` — V7-补丁 fail-closed 一致性修复
- 双推: GitHub ✅ Gitee ✅

## 最终自检结果
- `_constantTimeEqual` — 4 文件全覆盖 ✅
- `SecureStorageValidator` — 5 文件全覆盖 ✅
- POST 方法强制 — 6 个危险端点全覆盖 ✅
- fail-closed 策略 — 安全检测方法统一 ✅
- 空 Host 头 — 统一 false ✅
- Flutter analyze — 86 info (avoid_print only), 0 errors ✅
