# 2026-06-10 Vol.7 终极安全审计

## 完成事项
- Vol.7 白帽攻击：12 个漏洞修复 + 2 个自检补丁
- 安全评分从 9.8 提升到 **9.9/10**
- 累计 7 轮审计修复 **54 个漏洞**（10 CRITICAL + 14 HIGH + 14 MEDIUM + 16 LOW）

## Vol.7 修复清单
| 编号 | 严重度 | 修复内容 |
|------|--------|---------|
| V7-01 | HIGH | `/unpair` 缺少 POST 强制 — CSRF 可解绑所有设备 |
| V7-02 | HIGH | 空指纹绕过白名单 — `isEmpty → true` 逻辑修复 |
| V7-03 | HIGH | `getHardwareFingerprint()` 失败返回空串 — 改为 sentinel |
| V7-04 | HIGH | HMAC/AES 密钥复用 — HKDF-SHA256 派生独立 HMAC 密钥 |
| V7-05 | MEDIUM | `/pair` 405 返回纯文本 — 统一 JSON 格式 |
| V7-06 | MEDIUM | `_isTestMode()` 生产环境可用 — kDebugMode 编译时门控 |
| V7-07 | MEDIUM | `_clearActivation()` 不删 AES/HMAC 密钥 — 自毁后密钥残留 |
| V7-08 | MEDIUM | `_saveActivation()` 用 SharedPreferences — 迁移 FlutterSecureStorage |
| V7-09 | MEDIUM | DataChannel 无消息速率限制 — 添加 30msg/s 限流 |
| V7-10 | MEDIUM | `sendCommand()` 无出站校验 — 白名单验证 |
| V7-11 | MEDIUM | `roomId` 无输入验证 — 正则校验防信令注入 |
| V7-12 | MEDIUM | `isBeingDebugged()` fail-open — 改为 fail-closed |
| V7-自检-01 | MEDIUM | 空 Host 头绕过 DNS Rebinding 检查 — `isEmpty → false` |
| V7-补丁 | MEDIUM | DynamicFirewall.isBeingDebugged() catch 块与 V7-12 不一致 |

## Git 提交
- `f0826a1` / `0efff89` — Vol.7 主修复
- `65cb413` — V7-补丁 fail-closed 一致性修复
- 双推: GitHub ✅ Gitee ✅

## 最终自检结果
- `_constantTimeEqual` — 4 文件全覆盖 ✅
- `SecureStorageValidator` — 5 文件全覆盖 ✅
- POST 方法强制 — 6 个危险端点全覆盖 ✅
- fail-closed 策略 — 安全检测方法统一 ✅
- 空 Host 头 — 统一 false ✅
- Flutter analyze — 90 info (avoid_print only), 0 errors ✅

---

# 2026-06-10 发版 + 开发 + 实测

## 发版准备 (v1.0.3)
- Tag `v1.0.3` 已推 GitHub + Gitee
- CHANGELOG.md 已创建（v1.0.0 → v1.0.3 完整记录）
- GitHub Release 需从网页手动创建（gh CLI 未装）

## 跨网络模式增强 (v1.0.4-dev)
- 安全随机 roomId 生成：`WebRtcConnectionService.generateRoomId()` (Random.secure 8字符)
- 自动重连：3 次，指数退避 2s/4s/8s
- 连接超时：30s
- UI：随机生成按钮（🎲 图标）

## 代码混淆配置
- `scripts/build-release.sh` — 五端构建 + --obfuscate + --split-debug-info
- `scripts/build-windows.bat` — Windows 单独构建脚本
- `.gitignore` 添加 `build/debug-info/`
- Android ProGuard 已配置 (isMinifyEnabled + isShrinkResources)

## Windows EXE 实测 (22 项安全测试)
启动 `remote_pc.exe --headless` (SAFE_MODE=1, PID 23412)

| 测试 | 结果 |
|------|------|
| /ping 服务发现 | ✅ alive |
| GET /shutdown → 405 | ✅ CSRF 防护 |
| GET /restart → 405 | ✅ |
| GET /lock-screen → 405 | ✅ |
| GET /unpair → 405 (V7-01) | ✅ |
| POST 无 Token → 403 | ✅ |
| /auth-token 获取 Token | ✅ |
| GET + Token → 405 | ✅ |
| 无 Device ID → 403 | ✅ |
| 无 Fingerprint → 403 (V7-02) | ✅ |
| 非白名单设备 → 403 | ✅ |
| 速率限制 429 | ✅ |
| 设备配对 | ✅ |
| SAFE_MODE 拦截 /shutdown | ✅ |
| SAFE_MODE 拦截 /restart | ✅ |
| SAFE_MODE 拦截 /lock-screen | ✅ |
| SAFE_MODE 拦截 /remote-self-destruct | ✅ |
| 审计日志完整 | ✅ |
| x-test-mode 无效 (V7-06) | ✅ |
| 未知路径 → JSON 404 | ✅ |
| DNS Rebinding 防护 | ✅ |
| 已配对设备列表 | ✅ |

## Git 提交
- `eda3226` — CHANGELOG.md
- `84f425a` — v1.0.4-dev 跨网络增强 + 代码混淆 + 实测
- 双推: GitHub ✅ Gitee ✅
